require "spec_helper"

RSpec.describe Cf::Service::Tunneller do
  it "has a version number" do
    expect(Cf::Service::Tunneller::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
